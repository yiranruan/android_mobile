<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobileproject">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme">

        <activity android:name=".UserSetting" />
        <activity android:name=".tasks.ProgressActivity" />
        <activity android:name=".tasks.drawLinesActivity" />
        <activity android:name=".tasks.ShowTaskActivity" />
        <activity android:name=".tasks.LocationActivity" />
        <activity android:name=".tasks.MapActivity" />
        <activity android:name=".tasks.CreateButton" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC6jbrg0Qsu5MNJqUSiR6FMnmeY9G2nj7Y" />

        <activity android:name=".tasks.HorizontalCoordinatorNtbActivity" />
        <activity android:name=".group.GroupCreateActivity" />
        <activity android:name=".group.GroupJoinActivity" />
        <activity android:name=".group.ShowGroupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>