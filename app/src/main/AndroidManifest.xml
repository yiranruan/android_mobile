<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".tasks.HorizontalCoordinatorNtbActivity"></activity>
        <activity android:name=".tasks.HorizontalNtbActivity" />
        <activity android:name=".tasks.SamplesNtbActivity" />
        <activity android:name=".tasks.TopHorizontalNtbActivity" />
        <activity android:name=".tasks.VerticalNtbActivity" />
        <activity android:name=".tasks.TaskActivity" />
        <activity android:name=".group.GroupJoinActivity" />
        <activity android:name=".group.GroupCreateActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>